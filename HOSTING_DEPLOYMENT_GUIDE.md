# راهنمای جامع انتقال و راه‌اندازی سامانه ابری کارویتا روی هاست (Production Deployment Guide)

این راهنما شامل مراحل گام‌به‌گام و ساده برای استقرار پروژه، دیتابیس و تنظیمات در هاست‌های اشتراکی (سی‌پنل، دایرکت‌ادمین، نت‌افراز، پارس‌پک و ...) یا سرورهای اختصاصی/VPS می‌باشد.

---

## 📦 بسته آماده استقرار (یک فایل برای آپلود)
در ریشه پروژه، فایل فشرده آماده استقرار به نام زیر با حجم تنها **۱.۰۷ مگابایت** ایجاد شده است:
📁 **`karovita-production.zip`**

این بسته شامل تمامی فایل‌های کامپایل‌شده فرانت‌اند، اندپوینت‌های بک‌اند PHP، فایل ساختار پایگاه‌داده (`database.sql`) و قوانین وب‌سرور (`.htaccess`) است.

---

## 🚀 روش اول: راه‌اندازی سریع روی هاست‌های اشتراکی (cPanel / DirectAdmin)
این روش به هیچ‌گونه نرم‌افزار اضافی، کامپایل مجدد یا دستورات خط فرمان در هاست نیاز ندارد و با PHP نسخه‌های **7.4 تا 8.3+** کاملاً سازگار است.

### گام ۱: ساخت دیتابیس در هاست
1. وارد کنترل‌پنل هاست خود (مانند cPanel) شوید.
2. به بخش **MySQL Database Wizard** یا **Databases** بروید.
3. یک دیتابیس جدید بسازید (مثال: `karo_db`).
4. یک کاربر دیتابیس با رمز عبور قوی ایجاد کنید (مثال: `karo_user`).
5. کاربر را با دسترسی کامل (**ALL PRIVILEGES**) به دیتابیس متصل کنید.

### گام ۲: ایمپورت پایگاه‌داده (Database Import)
1. در سی‌پنل وارد ابزار **phpMyAdmin** شوید.
2. از ستون سمت چپ، روی نام دیتابیسی که در گام اول ساختید کلیک کنید.
3. از منوی بالا روی تب **Import** کلیک کنید.
4. فایل **`database.sql`** (موجود در ریشه پروژه یا داخل `dist/database.sql`) را انتخاب کرده و دکمه **Import / Go** در انتهای صفحه را بزنید.
> ✅ تمامی ۲۴ جدول استاندارد، کاربران اولیه، ۱۵ ماژول فعال ERP و اصناف با موفقیت در دیتابیس شما بارگذاری خواهند شد.

### گام ۳: آپلود فایل‌های پروژه در هاست
1. در سی‌پنل وارد **File Manager** شده و به پوشه **`public_html`** بروید.
2. روی دکمه **Upload** کلیک کنید و فایل **`karovita-production.zip`** را آپلود کنید.
3. روی فایل آپلود شده کلیک‌راست کرده و گزینه **Extract** را بزنید تا فایل‌ها مستقیماً در `public_html` باز شوند.
> 💡 نکته: مطمئن شوید فایل `index.html` و پوشه `api` و فایل‌های `.htaccess` مستقیماً داخل `public_html` قرار گرفته باشند (نه داخل یک پوشه اضافه).

### گام ۴: تنظیم مشخصات دیتابیس در فایل `.env`
1. در پوشه `public_html`، فایلی به نام **`.env.example`** قرار دارد.
2. نام این فایل را به **`.env`** تغییر دهید (Rename).
3. روی فایل `.env` کلیک‌راست کرده و گزینه **Edit** را انتخاب کنید و مشخصات دیتابیس ساخته شده در گام اول را وارد نمایید:
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=نام_دیتابیس_شما
DB_USER=نام_کاربری_دیتابیس_شما
DB_PASS=رمز_عبور_دیتابیس_شما

# کلیدهای امنیتی و درگاه‌ها (اختیاری - از داخل پنل مدیریت نیز قابل تغییرند)
JWT_SECRET=karovita_secure_jwt_key_938472918472
ZIBAL_MERCHANT=zibal
ZIBAL_SANDBOX=0
SMS_DRIVER=sms_ir
SMS_IR_API_KEY=ocv39CACg6Vg3cg3DbY3mUwfOti7dktYUwksl3jA3Jt1qI0z
SMS_IR_LINE_NUMBER=30007732
SMS_IR_TEMPLATE_ID=418155
SMS_IR_PARAM_NAME=CODE
```
4. تغییرات را ذخیره کنید (**Save Changes**).

### گام ۵: تست و بررسی آنلاین سامانه
- **تست سلامت اتصال و دیتابیس:** آدرس `https://yourdomain.com/api/health` را در مرورگر باز کنید. باید پیام وضعیت سبز (`status: ok`) همراه با زمان تاخیر اتصال به دیتابیس نمایش داده شود.
- **ورود به سامانه:** آدرس سایت خود را باز کرده و با شماره مالک و مدیر ارشد سیستم (**`09111273476`**) وارد شوید تا به تمامی ابزارهای مدیریت، سیستم قیمت‌گذاری، درگاه‌ها و کاربران دسترسی کامل داشته باشید.

---

## 🖥️ روش دوم: استقرار روی سرور مجازی یا هاست‌های ابری نودجی‌اس (Node.js / VPS)
اگر تمایل دارید پروژه را روی محیط اجرایی **Node.js** و با وب‌سرور اکسپرس اجرا کنید:

1. فایل **`dist/server.cjs`** فایل کامپایل‌شده سرور نود می‌باشد.
2. با دستور زیر سرور را در حالت پروداکشن روشن کنید:
```bash
node dist/server.cjs
```
یا با ابزار پایدار **PM2**:
```bash
pm2 start dist/server.cjs --name karovita-erp
pm2 save
pm2 startup
```
3. پورت پیش‌فرض پروژه `3000` است. می‌توانید با Nginx یا قابلیت Reverse Proxy سی‌پنل، دامنه خود را به پورت `3000` متصل کنید.

---

## 🛡️ ویژگی‌های مهندسی و خودترمیمی دیتابیس در هاست
- **مایگریشن خودکار (Self-Healing `v9_enterprise_standards_complete`):** سیستم به گونه‌ای کدنویسی شده است که به محض برقراری اولین ارتباط با دیتابیس، صحت تمام ۲۴ جدول و ستون‌های ۱۲ گانه استاندارد (Primary Key, created_at, updated_at, deleted_at, status, is_active) را بررسی کرده و در صورت نیاز بدون پاک شدن داده‌ها خود را ترمیم می‌کند.
- **فال‌بک پایدار (Zero-Downtime Fallback):** حتی در صورت بروز اختلال مقطعی در دیتابیس هاست، موتور داده سامانه به صورت خودکار از مخزن پایدار `db.json` سرویس‌دهی می‌کند تا صفحه به هیچ عنوان سفید یا مختل نشود.
- **مدیریت کامل درگاه‌ها در پنل ادمین:** شما می‌توانید بعد از بالا آمدن سایت، بدون دست زدن به کدها، از طریق تب **«درگاه شاپرک و SMS.ir»** در پنل ادمین، مرچنت کد واقعی زیبال و شناسه‌های پترن‌های پیامکی خود را تغییر دهید یا در صورت نیاز پاک نمایید.
